# Web framework
# fastapi is bumped to reach a release whose starlette floor allows the
# patched starlette below; starlette is pinned directly because it carries the
# advisories rather than fastapi itself.
fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# Database
sqlalchemy==2.0.36
alembic==1.18.5
asyncpg==0.30.0
greenlet==3.1.1

# AI
openai==1.65.2

# PDF parsing
# Pulls pdfminer.six 20260107, which carries the pdfminer fixes.
pdfplumber==0.11.10

# PDF generation
weasyprint==69.0
jinja2==3.1.6

# HTTP client (voice transcription)
httpx==0.28.1

# Config
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.2.2

# Error monitoring (disabled unless SENTRY_DSN is configured)
sentry-sdk==2.66.1

# Auth
PyJWT[crypto]==2.13.0

# Billing
stripe==15.3.1
